spring:
  application:
    name: myvedio

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://47.99.75.148:3306/myvideo
    hikari:
      max-lifetime: 300000

  datasource.hikari:
    maximum-pool-size: 10

  mybatis-plus:
    mapper-locations: classpath*:mapper/*.xml
    type-aliases-package: com.daiweij.modules.sys.entity